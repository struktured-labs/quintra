# Quintra v0.18.80 — Long Roads

The dungeons now feel as large as their Compass footprint. Every node before
the final merchant, sanctuary, and authored Colossus arena is a full
31×31-tile scrolling field. Stage one is a sustained seventeen-field
expedition and the Void route reaches twenty-seven; campaign-wide coverage
grows from 119 to 192 wide fields without adding room counters.

Each field keeps independent seed-stable geometry, combat, objectives, puzzles,
and map identity while adjacent nodes stream continuously through the CGB
background ring. The LCD and champion remain visible and stage music keeps
advancing. The final three-role cadence deliberately contracts so the
merchant, sanctuary, and boss threshold read as a destination.

Sigils, Waystones, and Wardens remain near the readable entry sector of their
larger landscapes. The nonlinear room-2/room-8 Rift and its body-wide route are
restamped after wide stage architecture, preserving the confusing seed-stable
shortcut instead of erasing it. The restoration lives in the world-generation
bank, leaving 1,097 bytes free in the tighter procgen bank.

Live-ROM coverage traverses all seventeen opening fields and verifies the
Rift, objective gates, puzzles, miniboss placement, reciprocal arrival,
collision, distributed combat, rotated Compass resume, and exact 31×31 VRAM
publication. A complete wide transition settles in 55 frames with four music
rows advancing. No room count, enemy HP, projectile, boss, hero, or difficulty
value changed.

The acreage audit also closed two real wide-arrival failures. A west-door
arrival in the extension strip can no longer suppress the required room-9
Warden, so its persistent trial gate cannot soft-lock an otherwise empty
field. The controller verifier now uses live 248×248 bounds and exact
firing-lane collision when pursuing a Mirror Moth or collecting a far-sector
Crystal relic, instead of mistaking the old 160×136 viewport edge for the
world edge.

An input-only Easy Picsean campaign completes all nine Colossi and reaches the
ending at frame 154,986 on seed 2064128163 with 11 HP. It collects every
observed intermediate boss relic inside the unchanged 210,000-frame budget,
and a fresh emulator reproduces the victory from the exact recorded buttons.
