# Quintra v0.18.75 — True Bearings

The Spirit Compass now identifies its dungeon stage as `S1 MAP` through
`S9 MAP`. The heading remains tile-native and leaves the full 6×5 pocket grid,
semantic legend, explored-route contrast, Sigil/trial/rift cues, and amber
pre-entry boss hint visible at once.

The external Normal room audit is more trustworthy too. Corvin and Picsean's
controller-only trainer now takes brief, cardinal firing beats while kiting a
close pack instead of fleeing forever without attacking. The corrected
45-room sample resolves 29 fixtures with zero deaths; Golden Temple Picsean
finds its open exit at 15/16 HP rather than dying against untouched enemies.
This changes no Normal enemy, player, procgen, or combat value.

All PyBoy, native mGBA, and five-minute deep-test checkpoints are regenerated
and hash-bound to the final cartridge.
