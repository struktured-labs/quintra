# Quintra v0.18.70 — Riftwild Deepens

Riftwild now scrolls vertically as well as horizontally. Every one of its
sixteen logical cells is a 224×200 generated world behind the Game Boy
Color's 160×136 playfield viewport, with a real 0–64px camera on both axes.
The former y=136 screen edge is traversable terrain; only the true y=184
threshold follows a southern graph link.

The southern field is first-class gameplay space. It has collision, paths,
cover, a third seed-stable landmark cluster, and encounter pressure in the
southeast beyond both old seams. Player movement, melee, projectiles, enemy
AI, rendering, camera state, checkpoints, and the input-only balance pilot all
use the same 224×200 coordinates. Across its 4×4 graph, Riftwild now covers
896×800 logical pixels before counting nonlinear Rift Wells.

The renderer streams the full 28×25 background field, and a dedicated
two-axis entity projection keeps the ordinary single-screen path within its
dense-frame budget. Outdoor reciprocal arrivals preserve the champion at the
correct far camera bound, including south-to-north travel.

Live-ROM coverage traverses every cell, reaches the southeast camera bound,
checks all three landmark regions and both obsolete seams, follows the cave
vault and dungeon gate, and retains the 370 hash-bound checkpoint curriculum.
The complete build, media, boss, enemy, puzzle, music, controller, performance,
full-victory, and reproducibility gates pass.

Ordinary dungeon rooms remain 160×136. This release meaningfully expands the
outdoor fields without claiming the later larger-dungeon-region milestone is
already complete.
